# Sửa hồ sơ trong Cài đặt — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cho người dùng sửa Tên hiển thị và Ngày bắt đầu tháng trong màn Cài đặt qua một bottom sheet.

**Architecture:** Thêm `updateProfile` vào tầng dữ liệu (interface `Repo` + 2 bản cài đặt), một hook `useUpdateProfile`, và một component `ProfileEditSheet` mở từ `SettingsPage`. Loại tiền gốc chỉ hiển thị, khóa lại. Một hàm thuần `clampMonthStartDay` giữ giá trị ngày trong 1–28, có unit test.

**Tech Stack:** React 19, TanStack Query, TypeScript, Tailwind, Vitest. Dữ liệu qua interface `Repo` (demoRepo localStorage / supabaseRepo Postgres).

## Global Constraints

- `month_start_day` phải nằm trong 1–28 (ràng buộc CHECK ở DB; tháng luôn có ngày này).
- Mọi đọc/ghi dữ liệu đi qua interface `Repo` — không gọi thẳng Supabase/localStorage từ UI.
- **Không** cho sửa `base_currency` — `ProfilePatch` cố ý loại bỏ trường này.
- Text tiếng Việt, phong cách gọn giống các sheet sẵn có (`BudgetEditSheet`).

---

### Task 1: Hàm thuần `clampMonthStartDay` + test

**Files:**
- Modify: `src/lib/dates.ts` (thêm hàm cuối file)
- Test: `src/lib/dates.test.ts` (thêm block mô tả mới)

**Interfaces:**
- Produces: `clampMonthStartDay(n: number): number` — kẹp về 1–28, làm tròn, giá trị không hữu hạn → 1.

- [ ] **Step 1: Viết test thất bại**

Thêm vào cuối `src/lib/dates.test.ts` (giữ nguyên import `describe/it/expect` sẵn có; nếu file chưa import thì thêm `import { describe, it, expect } from 'vitest'` ở đầu, và thêm `clampMonthStartDay` vào dòng import từ `'./dates'`):

```ts
describe('clampMonthStartDay', () => {
  it('giữ nguyên giá trị hợp lệ', () => {
    expect(clampMonthStartDay(1)).toBe(1)
    expect(clampMonthStartDay(15)).toBe(15)
    expect(clampMonthStartDay(28)).toBe(28)
  })
  it('kẹp về biên', () => {
    expect(clampMonthStartDay(0)).toBe(1)
    expect(clampMonthStartDay(-5)).toBe(1)
    expect(clampMonthStartDay(29)).toBe(28)
    expect(clampMonthStartDay(31)).toBe(28)
  })
  it('làm tròn số thập phân', () => {
    expect(clampMonthStartDay(15.6)).toBe(16)
  })
  it('giá trị không hữu hạn → 1', () => {
    expect(clampMonthStartDay(NaN)).toBe(1)
  })
})
```

- [ ] **Step 2: Chạy test để chắc chắn nó fail**

Run: `npm test -- dates`
Expected: FAIL — `clampMonthStartDay is not a function` / không export.

- [ ] **Step 3: Cài đặt hàm tối thiểu**

Thêm vào cuối `src/lib/dates.ts`:

```ts
/** Kẹp ngày bắt đầu tháng về 1–28 (làm tròn; giá trị không hữu hạn → 1). */
export function clampMonthStartDay(n: number): number {
  if (!Number.isFinite(n)) return 1
  return Math.min(28, Math.max(1, Math.round(n)))
}
```

- [ ] **Step 4: Chạy test để chắc chắn nó pass**

Run: `npm test -- dates`
Expected: PASS (tất cả case trong `clampMonthStartDay` + các test dates cũ vẫn xanh).

- [ ] **Step 5: Commit**

```bash
git add src/lib/dates.ts src/lib/dates.test.ts
git commit -m "GD-hoso: them clampMonthStartDay (ke ve 1-28)"
```

---

### Task 2: Tầng dữ liệu `updateProfile` + hook `useUpdateProfile`

**Files:**
- Modify: `src/data/repo.ts` (thêm type `ProfilePatch` + method vào interface `Repo`)
- Modify: `src/data/supabaseRepo.ts` (thêm `updateProfile`)
- Modify: `src/data/demoRepo.ts` (thêm `updateProfile`)
- Modify: `src/hooks/queries.ts` (thêm `useUpdateProfile`)

**Interfaces:**
- Consumes: `ProfileRow` từ `src/types/database.types.ts`.
- Produces:
  - `type ProfilePatch = Partial<Pick<ProfileRow, 'display_name' | 'month_start_day'>>`
  - `repo.updateProfile(patch: ProfilePatch): Promise<ProfileRow>`
  - `useUpdateProfile()` → mutation với `mutateAsync(patch: ProfilePatch)`.

- [ ] **Step 1: Thêm type + method vào interface `Repo`**

Trong `src/data/repo.ts`, thêm sau import `ProfileRow` đã có, đặt gần các type patch khác (ví dụ ngay trên `export interface Repo`):

```ts
/** Chỉ sửa được tên hiển thị + ngày bắt đầu tháng. Cố ý KHÔNG có base_currency. */
export type ProfilePatch = Partial<Pick<ProfileRow, 'display_name' | 'month_start_day'>>
```

Rồi trong `export interface Repo`, ngay dưới dòng `getProfile(): Promise<ProfileRow>`:

```ts
  updateProfile(patch: ProfilePatch): Promise<ProfileRow>
```

- [ ] **Step 2: Cài `updateProfile` cho supabaseRepo**

Trong `src/data/supabaseRepo.ts`, thêm ngay dưới `async getProfile() {...},`:

```ts
  async updateProfile(patch) {
    const uid = await currentUserId()
    const { data, error } = await getSupabase()
      .from('profiles')
      .update(patch)
      .eq('user_id', uid)
      .select()
      .single()
    if (error) throw error
    return data
  },
```

- [ ] **Step 3: Cài `updateProfile` cho demoRepo**

Trong `src/data/demoRepo.ts`, thêm ngay dưới `async getProfile() {...},`:

```ts
  async updateProfile(patch) {
    const db = load()
    db.profile = { ...db.profile, ...patch }
    save(db)
    return db.profile
  },
```

- [ ] **Step 4: Thêm hook `useUpdateProfile`**

Trong `src/hooks/queries.ts`: thêm `ProfilePatch` vào dòng import type từ `'../data/repo'` (cùng chỗ đang import `CategoryPatch`, `NewAccount`…). Rồi thêm hook (đặt gần `useProfile`/đầu file, sau `export { useProfile }`):

```ts
export function useUpdateProfile() {
  const qc = useQueryClient()
  return useMutation({
    mutationFn: (patch: ProfilePatch) => repo.updateProfile(patch),
    onSettled: () => qc.invalidateQueries({ queryKey: ['profile'] }),
  })
}
```

(`useMutation`, `useQueryClient`, `repo` đã được import sẵn trong file này.)

- [ ] **Step 5: Kiểm tra biên dịch**

Run: `npm run build`
Expected: PASS — không lỗi TypeScript (cả 2 repo đã cài đủ method của interface `Repo`).

- [ ] **Step 6: Commit**

```bash
git add src/data/repo.ts src/data/supabaseRepo.ts src/data/demoRepo.ts src/hooks/queries.ts
git commit -m "GD-hoso: updateProfile o tang du lieu + hook useUpdateProfile"
```

---

### Task 3: `ProfileEditSheet` + nối vào `SettingsPage`

**Files:**
- Create: `src/features/settings/ProfileEditSheet.tsx`
- Modify: `src/features/settings/SettingsPage.tsx`

**Interfaces:**
- Consumes: `useUpdateProfile` (Task 2), `clampMonthStartDay` (Task 1), `CURRENCIES` từ `src/lib/money.ts`, `ProfileRow`.
- Produces: `ProfileEditSheet({ profile, onClose })`.

- [ ] **Step 1: Tạo component `ProfileEditSheet`**

Tạo `src/features/settings/ProfileEditSheet.tsx`:

```tsx
import { useState } from 'react'
import { useUpdateProfile } from '../../hooks/queries'
import { clampMonthStartDay } from '../../lib/dates'
import { CURRENCIES } from '../../lib/money'
import type { ProfileRow } from '../../types/database.types'

interface Props {
  profile: ProfileRow
  onClose: () => void
}

const DAY_OPTIONS = Array.from({ length: 28 }, (_, i) => i + 1)

/** Sheet sửa tên hiển thị + ngày bắt đầu tháng. Loại tiền gốc chỉ hiển thị. */
export function ProfileEditSheet({ profile, onClose }: Props) {
  const update = useUpdateProfile()
  const [name, setName] = useState(profile.display_name ?? '')
  const [day, setDay] = useState(clampMonthStartDay(profile.month_start_day))

  async function handleSave() {
    await update.mutateAsync({
      display_name: name.trim() || null,
      month_start_day: clampMonthStartDay(day),
    })
    onClose()
  }

  return (
    <div
      className="fixed inset-0 z-30 flex items-end justify-center bg-black/40 lg:items-center"
      onClick={onClose}
    >
      <div
        className="w-full max-w-lg rounded-t-2xl bg-gray-50 p-4 lg:rounded-2xl"
        onClick={(e) => e.stopPropagation()}
      >
        <div className="mb-3 flex items-center justify-between">
          <h2 className="text-base font-bold text-gray-800">Hồ sơ</h2>
          <button
            type="button"
            onClick={onClose}
            className="rounded-lg px-3 py-1.5 text-sm text-gray-500 hover:bg-gray-100"
          >
            Đóng
          </button>
        </div>

        <label className="block text-xs font-medium text-gray-500">Tên hiển thị</label>
        <input
          autoFocus
          value={name}
          onChange={(e) => setName(e.target.value)}
          placeholder="Tên của bạn"
          className="mt-1 w-full rounded-xl border border-gray-300 bg-white p-3 text-gray-800 focus:border-green-500 focus:outline-none"
        />

        <label className="mt-3 block text-xs font-medium text-gray-500">Ngày bắt đầu tháng</label>
        <select
          value={day}
          onChange={(e) => setDay(Number(e.target.value))}
          className="mt-1 w-full rounded-xl border border-gray-300 bg-white p-3 text-gray-800 focus:border-green-500 focus:outline-none"
        >
          {DAY_OPTIONS.map((d) => (
            <option key={d} value={d}>
              Ngày {d}
            </option>
          ))}
        </select>
        <p className="mt-1 text-xs text-gray-400">Ảnh hưởng cách tính tháng trong báo cáo.</p>

        <label className="mt-3 block text-xs font-medium text-gray-500">Loại tiền gốc</label>
        <input
          value={`${profile.base_currency} · ${CURRENCIES[profile.base_currency].label}`}
          disabled
          className="mt-1 w-full rounded-xl border border-gray-200 bg-gray-100 p-3 text-gray-400"
        />
        <p className="mt-1 text-xs text-gray-400">Không đổi được.</p>

        <button
          type="button"
          onClick={handleSave}
          disabled={update.isPending}
          className="mt-4 w-full rounded-xl bg-green-600 py-3 text-sm font-semibold text-white active:scale-[0.99] disabled:opacity-60"
        >
          Lưu
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Nối vào `SettingsPage`**

Trong `src/features/settings/SettingsPage.tsx`:

(a) Thêm import ở đầu file:

```tsx
import { useState } from 'react'
import { ProfileEditSheet } from './ProfileEditSheet'
```

(b) Thêm state trong đầu hàm `SettingsPage` (sau các dòng `const { data: ... } = ...`):

```tsx
  const [editing, setEditing] = useState(false)
```

(c) Thay nguyên `<section>` "Tài khoản đăng nhập" (khối bắt đầu bằng `<h2 ...>Tài khoản đăng nhập</h2>`) bằng:

```tsx
      <section className="overflow-hidden rounded-xl bg-white shadow-sm">
        <button
          type="button"
          onClick={() => setEditing(true)}
          className="flex w-full items-center gap-3 px-3 py-3 text-left hover:bg-gray-50"
        >
          <span className="text-xl">👤</span>
          <span className="flex-1">
            <span className="block text-sm text-gray-800">{profile?.display_name ?? '—'}</span>
            <span className="block text-xs text-gray-400">
              Tháng bắt đầu ngày {profile?.month_start_day ?? 1} · Tiền gốc {profile?.base_currency ?? '—'}
            </span>
          </span>
          <span className="text-gray-300">›</span>
        </button>
        {!isDemoMode && (
          <div className="border-t border-gray-100 px-3 py-3">
            <button
              type="button"
              onClick={() => getSupabase().auth.signOut()}
              className="rounded-lg border border-gray-300 px-3 py-1.5 text-sm text-gray-600 hover:bg-gray-100"
            >
              Đăng xuất
            </button>
          </div>
        )}
      </section>
```

(d) Ngay trước thẻ đóng `</div>` ngoài cùng của return, thêm render sheet:

```tsx
      {editing && profile && <ProfileEditSheet profile={profile} onClose={() => setEditing(false)} />}
```

- [ ] **Step 3: Kiểm tra biên dịch + lint**

Run: `npm run build`
Expected: PASS — không lỗi TypeScript.

Run: `npm run lint`
Expected: PASS (không lỗi mới).

- [ ] **Step 4: Kiểm thử tay trên dev server**

Mở dev server (preview), vào Cài đặt → bấm mục Hồ sơ (👤). Kiểm tra:
- Sheet mở lên, hiện đúng tên + ngày + tiền gốc (tiền gốc bị làm mờ).
- Đổi tên và ngày → bấm Lưu → sheet đóng, mục Hồ sơ hiển thị giá trị mới.
- Mở lại Báo cáo: nếu đổi ngày bắt đầu tháng, khoảng tháng thay đổi tương ứng.

- [ ] **Step 5: Commit**

```bash
git add src/features/settings/ProfileEditSheet.tsx src/features/settings/SettingsPage.tsx
git commit -m "GD-hoso: ProfileEditSheet + noi vao trang Cai dat"
```

---

## Self-Review

**Spec coverage:**
- Tầng dữ liệu (`ProfilePatch` + `updateProfile` 2 repo) → Task 2. ✔
- Hook `useUpdateProfile` → Task 2. ✔
- `ProfileEditSheet` (tên, ngày 1–28, tiền gốc khóa) → Task 3. ✔
- `SettingsPage` mục "Hồ sơ" bấm mở sheet, giữ Đăng xuất → Task 3. ✔
- `clampMonthStartDay` + test → Task 1. ✔
- Khóa `base_currency` → `ProfilePatch` không có trường này (Task 2). ✔

**Placeholder scan:** Không có TBD/TODO; mọi bước có code hoặc lệnh cụ thể. ✔

**Type consistency:** `ProfilePatch`, `updateProfile`, `useUpdateProfile`, `clampMonthStartDay`, `ProfileEditSheet({ profile, onClose })` dùng nhất quán giữa các task. ✔
