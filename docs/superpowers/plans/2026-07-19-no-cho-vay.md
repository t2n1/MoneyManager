# Plan: Chức năng Nợ / cho vay (mục F)

> Spec: [`../specs/2026-07-19-no-cho-vay-design.md`](../specs/2026-07-19-no-cho-vay-design.md)
> Sau khi xong: `npm run build` + `npm run lint` + `npm test` sạch; 1 commit, message không dấu.

## Task 1 — Migration + types
- `supabase/migrations/0007_debts.sql`: bảng `debts`, `debt_payments`, RLS `"own rows"`,
  index, trigger `moddatetime` cho `debts`.
- `src/types/database.types.ts`: `DebtRow`, `DebtPaymentRow`, `DebtDirection`,
  `DebtStatus` + entry trong `Database.Tables`.

## Task 2 — Repo interface + 2 impl
- `src/data/repo.ts`: `NewDebt`, `DebtPatch`, `NewDebtPayment` + 7 method vào `Repo`.
- `src/data/demoRepo.ts`: mảng `debts`/`debtPayments` trong `DemoDB`, seed vài khoản
  mẫu, bump `STORAGE_KEY` → v9, cài 7 method (tạo/xóa giao dịch liên kết in-memory).
- `src/data/supabaseRepo.ts`: cài 7 method (payment kèm transaction: insert tx rồi payment).
- `src/data/index.ts`: export type mới.

## Task 3 — Helper thuần + test
- `src/features/debts/aggregate.ts`: `remainingOf`, `debtSummary`.
- `src/features/debts/aggregate.test.ts`: remaining, quy đổi đa tệ, thiếu tỷ giá, lọc settled.

## Task 4 — Hooks
- `src/hooks/queries.ts`: `useDebts`, `useDebtPayments`, `useCreateDebt`,
  `useUpdateDebt`, `useDeleteDebt`, `useCreateDebtPayment`, `useDeleteDebtPayment`.
  Payment đụng giao dịch → invalidate `['debts','debtPayments','transactions','balances']`.

## Task 5 — UI
- `src/features/debts/DebtsPage.tsx` (`/settings/debts`): tổng quan (mình nợ / cho
  vay), danh sách theo chiều, nút + Thêm → `DebtFormSheet`.
- `src/features/debts/DebtDetailPage.tsx` (`/settings/debts/:debtId`): thông tin khoản
  nợ, còn nợ, lịch sử trả, nút Ghi nhận trả → `DebtPaymentSheet`, đánh dấu tất toán, xóa.
- `src/features/debts/DebtFormSheet.tsx`, `DebtPaymentSheet.tsx`.
- Route lazy trong `src/App.tsx`; link "Nợ / cho vay 🤝" trong `SettingsPage`.

## Task 6 — Tài sản ròng
- `src/features/assets/AssetsPage.tsx`: khối "Tài sản ròng" dùng `debtSummary`.

## Verify
- `npm run build` + `npm run lint` + `npm test`.
- Chạy demo (Browser pane): tạo khoản nợ, ghi nhận trả (có/không chuyển tiền), kiểm số
  dư tài khoản đổi đúng khi có chuyển tiền, tài sản ròng khớp, xóa payment hoàn số dư.
