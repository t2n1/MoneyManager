# Chuyến đi (mục 2a) — kế hoạch thi công

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tháng có chuyến đi thôi đánh lừa — app tự dò dải ngày trống, hỏi xác nhận, rồi loại
những ngày đi vắng khỏi mọi mốc so sánh trong khi biểu đồ vẫn hiện đủ kèm dấu.

**Architecture:** Bảng `trips` mới (kèm cột `dismissed` nhớ lần bỏ qua). Một module thuần
`ngayDiVang.ts` vừa dò dải trống vừa sinh tập ngày/tháng cần loại. Hai hàm mốc-so trong
`aggregate.ts` nhận thêm tham số tập-loại-trừ có mặc định rỗng (= hành vi cũ); `periodCompare`
và `forecastMonthEnd` thuần toán, **không đổi** — chỉ đổi chỗ dựng đầu vào cho chúng.

**Tech Stack:** TypeScript · React · Vitest · Supabase (Postgres + RLS) · Recharts

**Spec:** [docs/superpowers/specs/2026-09-05-chuyen-di-design.md](../specs/2026-09-05-chuyen-di-design.md)

## Global Constraints

- **Đổi schema là đổi hai file cùng commit:** migration + [src/types/database.types.ts](../../../src/types/database.types.ts)
  (viết tay, không codegen — quên là compiler im, query chết lúc chạy).
- Hai bản `Repo` (`supabaseRepo`, `demoRepo`) phải cùng thoả interface; mỗi `mutationFn` có
  invalidation nằm ngay cạnh nó trong `hooks/queries.ts`.
- **Không sửa** `periodCompare`, `forecastMonthEnd`, `monthlySeries`. Không đụng `src/mcp/`,
  không chạy `bundle:mcp`/`bundle:rules`.
- Tham số loại-trừ mới trên `categoryComparison` / `monthExpenseCompare` **mặc định tập
  rỗng** = hành vi cũ — mọi phép thử hiện có phải xanh nguyên vẹn, đổi một phép thử cũ nào là
  sai hướng.
- Mọi thứ theo tháng đi qua `getMonthRange(key, monthStartDay)` — không tự tính đầu tháng.
- Mọi con số ra màn hình qua `<Money>` / `<Num>`. Không chêm giá trị tuỳ ý vào class Tailwind.
- **KHÔNG bọc `<Guide>` quanh câu chú thích tháng** — Guide trả `null` ở chế độ Gọn (mặc
  định của app). Đây là lỗi đã mắc ở mục 1, xem `ChiChuaGhiLine.tsx`.
- Kiểm: `npm run build` (`tsc -b`, KHÔNG dùng `tsc --noEmit`), `npm test`, `npm run lint`.
  Không chạy prettier.
- Nhánh làm việc: tạo `feat/chuyen-di` từ `master` trước khi sửa gì.

---

### Task 1: Tầng dữ liệu — bảng `trips` đi qua đủ năm tầng

**Files:**
- Create: `supabase/migrations/0058_trips.sql`
- Modify: `src/types/database.types.ts` (thêm `TripRow` cạnh `RelativeRow` ~dòng 205, và mục
  `trips:` trong `Database` cạnh `relatives:` ~dòng 1206)
- Modify: `src/data/repo.ts` (interface + `NewTrip`, đặt cạnh khối relatives ~dòng 663)
- Modify: `src/data/supabaseRepo.ts` (cạnh `getRelatives` ~dòng 772)
- Modify: `src/data/demoRepo.ts` (store key ~dòng 257 + methods cạnh `getRelatives` ~dòng 1676)
- Modify: `src/hooks/queries.ts` (cạnh `useRelatives` ~dòng 756)
- Test: `src/data/demoRepo.test.ts` (thêm describe mới)

**Interfaces:**
- Consumes: khuôn `relatives` (migration 0056) ở cả năm tầng — chép cấu trúc, đổi ruột.
- Produces: `TripRow`, `NewTrip`, `repo.listTrips(): Promise<TripRow[]>`,
  `repo.createTrip(input: NewTrip): Promise<TripRow>`, `repo.deleteTrip(id: string):
  Promise<void>`, hooks `useTrips()` / `useCreateTrip()` / `useDeleteTrip()` — Task 2–5 dùng.

- [ ] **Step 1: Migration**

Tạo `supabase/migrations/0058_trips.sql` — nguyên văn DDL ở spec §4 (đã gồm cột `dismissed`):

```sql
-- Chuyến đi (spec 2026-09-05-chuyen-di): dải ngày người dùng đi vắng.
-- `dismissed = true` KHÔNG phải một chuyến — nó là trí nhớ "đã hỏi về dải này rồi,
-- người dùng nói không phải", để app không hỏi lại. Mọi phép tính chỉ lấy dismissed = false.
create table if not exists public.trips (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users (id) on delete cascade,
  start_on date not null,
  end_on date not null,
  label text not null default '',
  -- ISO-2 nơi đến. Đợt 1 chưa tính gì từ nó; đợt 2b cần để biết quy đổi sang tiền nào.
  country text not null default 'VN',
  dismissed boolean not null default false,
  created_at timestamptz not null default now(),
  constraint trips_range_ok check (end_on >= start_on)
);

create index if not exists trips_user_idx on public.trips (user_id, start_on);

alter table public.trips enable row level security;
drop policy if exists "own rows" on public.trips;
create policy "own rows" on public.trips
  for all using (auth.uid() = user_id) with check (auth.uid() = user_id);
```

- [ ] **Step 2: `database.types.ts` — CÙNG BƯỚC với migration**

Thêm cạnh `RelativeRow`:

```ts
/** Chuyến đi (migration 0058). `dismissed = true` = dải đã hỏi, KHÔNG phải chuyến đi. */
export type TripRow = {
  id: string
  user_id: string
  start_on: string
  end_on: string
  label: string
  country: string
  dismissed: boolean
  created_at: string
}
```

Và trong `Database` (cạnh mục `relatives:`), theo đúng khuôn `InsertOf` tại chỗ:

```ts
      trips: {
        Row: TripRow
        Insert: InsertOf<
          TripRow,
          'user_id' | 'start_on' | 'end_on',
          'id' | 'label' | 'country' | 'dismissed' | 'created_at'
        >
        Update: Partial<TripRow>
      }
```

Mở khuôn `relatives:` ngay trên đó xem `InsertOf` có đúng ba tham số kiểu như vậy không —
nếu nó còn `Relationships:` hay trường khác thì chép đủ theo nó.

- [ ] **Step 3: repo.ts — kiểu và interface**

```ts
export interface NewTrip {
  start_on: string
  end_on: string
  /** Bỏ trống = ''. */
  label?: string
  /** Bỏ trống = 'VN'. */
  country?: string
  /** true = "đã hỏi, không phải chuyến đi". Bỏ trống = false. */
  dismissed?: boolean
}
```

Trong interface `Repo`, cạnh khối relatives:

```ts
  // --- Chuyến đi (migration 0058) ---
  listTrips(): Promise<TripRow[]>
  createTrip(input: NewTrip): Promise<TripRow>
  deleteTrip(id: string): Promise<void>
```

- [ ] **Step 4: supabaseRepo.ts**

```ts
  async listTrips() {
    const { data, error } = await getSupabase().from('trips').select('*').order('start_on')
    if (error) throw error
    return data
  },

  async createTrip(input: NewTrip) {
    const user_id = await currentUserId()
    const { data, error } = await getSupabase()
      .from('trips')
      .insert({ ...input, user_id })
      .select()
      .single()
    if (error) throw error
    return data
  },

  async deleteTrip(id: string) {
    const { error } = await getSupabase().from('trips').delete().eq('id', id)
    if (error) throw error
  },
```

- [ ] **Step 5: demoRepo.ts**

Thêm `trips?: TripRow[]` vào kiểu store (~dòng 257, cạnh `relatives?:`). Seed KHÔNG thêm
chuyến nào — demo mở ra phải y hệt hôm nay. Methods:

```ts
  async listTrips() {
    return (load().trips ?? []).slice().sort((a, b) => a.start_on.localeCompare(b.start_on))
  },

  async createTrip(input: NewTrip) {
    const db = load()
    db.trips ??= []
    const row: TripRow = {
      id: uuid(),
      user_id: DEMO_USER,
      start_on: input.start_on,
      end_on: input.end_on,
      label: input.label ?? '',
      country: input.country ?? 'VN',
      dismissed: input.dismissed ?? false,
      created_at: nowISO(),
    }
    db.trips.push(row)
    save(db)
    return row
  },

  async deleteTrip(id: string) {
    const db = load()
    db.trips = (db.trips ?? []).filter((t) => t.id !== id)
    save(db)
  },
```

`save(db)` — mở `createRelative` trong cùng file xem tên hàm ghi store đúng là gì (có thể là
`persist`/`write`); dùng đúng tên đó, hai dòng trên chỉ là khuôn.

- [ ] **Step 6: queries.ts**

```ts
export function useTrips() {
  return useQuery({
    queryKey: ['trips'],
    queryFn: () => repo.listTrips(),
    staleTime: 5 * 60_000,
  })
}

function invalidateTrips(qc: ReturnType<typeof useQueryClient>) {
  qc.invalidateQueries({ queryKey: ['trips'] })
}

export function useCreateTrip() {
  const qc = useQueryClient()
  return useMutation({
    mutationFn: (input: NewTrip) => repo.createTrip(input),
    onSettled: () => invalidateTrips(qc),
  })
}

export function useDeleteTrip() {
  const qc = useQueryClient()
  return useMutation({
    mutationFn: (id: string) => repo.deleteTrip(id),
    onSettled: () => invalidateTrips(qc),
  })
}
```

- [ ] **Step 7: Viết phép thử round-trip cho demoRepo**

Thêm vào `src/data/demoRepo.test.ts` (theo đúng khuôn setup của các describe sẵn có trong
file — đọc describe đầu tiên để lấy cách reset store):

```ts
describe('trips', () => {
  it('create → list → delete tròn vòng', async () => {
    const t = await demoRepo.createTrip({ start_on: '2026-02-16', end_on: '2026-02-22' })
    expect(t.dismissed).toBe(false)
    expect(t.country).toBe('VN')
    const all = await demoRepo.listTrips()
    expect(all.map((x) => x.id)).toContain(t.id)
    await demoRepo.deleteTrip(t.id)
    expect((await demoRepo.listTrips()).map((x) => x.id)).not.toContain(t.id)
  })

  it('dismissed lưu được', async () => {
    const t = await demoRepo.createTrip({
      start_on: '2026-03-01',
      end_on: '2026-03-05',
      dismissed: true,
    })
    expect(t.dismissed).toBe(true)
  })
})
```

- [ ] **Step 8: Chạy kiểm**

```bash
npx vitest run src/data/demoRepo.test.ts
```

Kỳ vọng: PASS. Rồi:

```bash
npx tsc -b
```

Kỳ vọng: exit 0 — nếu quên method ở một bản repo thì đỏ ở đây, đúng thiết kế.

- [ ] **Step 9: Commit**

```bash
git add supabase/migrations/0058_trips.sql src/types/database.types.ts src/data/repo.ts src/data/supabaseRepo.ts src/data/demoRepo.ts src/data/demoRepo.test.ts src/hooks/queries.ts
git commit -m "feat(chuyen-di): bang trips di qua du nam tang du lieu"
```

**Lưu ý bàn giao:** migration chưa chạy trên Supabase thật ở task này — chạy lúc chốt (Task 6),
theo cách user vẫn deploy migration (hỏi user nếu chưa rõ đường áp migration production).

---

### Task 2: Toán thuần `ngayDiVang.ts` — dò dải trống + sinh tập loại trừ

**Files:**
- Create: `src/features/reports/ngayDiVang.ts`
- Test: `src/features/reports/ngayDiVang.test.ts`
- Modify: `src/features/reports/aggregate.ts:159` — thêm `export` trước `const monthId`

**Interfaces:**
- Consumes: `TripRow` (Task 1); `addDaysISO`, `monthKeyForDate` từ `../../lib/dates`;
  `monthId` từ `./aggregate` (dòng 159: `` const monthId = (k) => `${k.year}-${k.month}` `` —
  export ra để hai nơi không tự chế hai định dạng khoá tháng).
- Produces: `NGUONG_NGAY_VANG`, `ngayDiVang()`, `boNgayDiVang()`, `thangCoChuyenDi()`,
  `doKhoangVang()`, `KhoangVang` — Task 3, 4, 5 dùng.

- [ ] **Step 1: Viết phép thử thất bại**

Tạo `src/features/reports/ngayDiVang.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import type { TripRow } from '../../types/database.types'
import {
  NGUONG_NGAY_VANG,
  boNgayDiVang,
  doKhoangVang,
  ngayDiVang,
  thangCoChuyenDi,
} from './ngayDiVang'

let seq = 0
function trip(p: Partial<TripRow> & Pick<TripRow, 'start_on' | 'end_on'>): TripRow {
  return {
    id: `tr${seq++}`,
    user_id: 'u',
    label: '',
    country: 'VN',
    dismissed: false,
    created_at: '',
    ...p,
  }
}

describe('ngayDiVang', () => {
  it('một chuyến 7 ngày → đúng 7 ngày ISO, gồm cả hai đầu', () => {
    const s = ngayDiVang([trip({ start_on: '2026-02-16', end_on: '2026-02-22' })])
    expect(s.size).toBe(7)
    expect(s.has('2026-02-16')).toBe(true)
    expect(s.has('2026-02-22')).toBe(true)
    expect(s.has('2026-02-23')).toBe(false)
  })

  it('dismissed → KHÔNG vào tập', () => {
    const s = ngayDiVang([trip({ start_on: '2026-02-16', end_on: '2026-02-22', dismissed: true })])
    expect(s.size).toBe(0)
  })

  it('hai chuyến chồng ngày → không đếm trùng', () => {
    const s = ngayDiVang([
      trip({ start_on: '2026-02-16', end_on: '2026-02-20' }),
      trip({ start_on: '2026-02-19', end_on: '2026-02-22' }),
    ])
    expect(s.size).toBe(7)
  })
})

describe('boNgayDiVang', () => {
  const pts = [
    { date: '2026-02-15', expense: 100 },
    { date: '2026-02-16', expense: 0 },
    { date: '2026-02-17', expense: 0 },
    { date: '2026-02-23', expense: 200 },
  ]
  it('bỏ đúng ngày, giữ thứ tự', () => {
    const vang = ngayDiVang([trip({ start_on: '2026-02-16', end_on: '2026-02-22' })])
    expect(boNgayDiVang(pts, vang)).toEqual([
      { date: '2026-02-15', expense: 100 },
      { date: '2026-02-23', expense: 200 },
    ])
  })
  it('tập rỗng → giữ nguyên', () => {
    expect(boNgayDiVang(pts, new Set())).toEqual(pts)
  })
})

describe('thangCoChuyenDi', () => {
  it('chuyến vắt hai tháng → mỗi tháng đếm phần của mình theo monthStartDay', () => {
    // monthStartDay = 1: 28/2–3/3 = 1 ngày tháng 2 (28) + 3 ngày tháng 3 → chỉ tháng 2
    // KHÔNG đạt ngưỡng 4, tháng 3 cũng không.
    const t = [trip({ start_on: '2026-02-28', end_on: '2026-03-03' })]
    const s = thangCoChuyenDi(t, 1)
    expect(s.size).toBe(0)
  })
  it('tháng đủ ngưỡng thì vào tập, đúng định dạng khoá của aggregate', () => {
    const s = thangCoChuyenDi([trip({ start_on: '2026-02-16', end_on: '2026-02-22' })], 1)
    // monthId của aggregate là `${year}-${month}` KHÔNG độn số 0 → '2026-2'
    expect(s.has('2026-2')).toBe(true)
  })
  it('3 ngày — dưới ngưỡng → không vào', () => {
    const s = thangCoChuyenDi([trip({ start_on: '2026-02-16', end_on: '2026-02-18' })], 1)
    expect(s.size).toBe(0)
  })
})

describe('doKhoangVang', () => {
  // Cửa sổ 1/2 → hôm nay 28/2. Có giao dịch mỗi ngày TRỪ 16–22/2.
  const days: string[] = []
  for (let d = 1; d <= 28; d++) {
    const iso = `2026-02-${String(d).padStart(2, '0')}`
    if (iso < '2026-02-16' || iso > '2026-02-22') days.push(iso)
  }

  it('dải 7 ngày trống, đã đóng → tìm ra đúng một dải', () => {
    const gaps = doKhoangVang(days, '2026-02-01', '2026-02-28', [])
    expect(gaps).toEqual([{ startISO: '2026-02-16', endISO: '2026-02-22', soNgay: 7 }])
  })

  it('dải 3 ngày — dưới ngưỡng → không báo', () => {
    const short = days.filter((d) => d < '2026-02-10' || d > '2026-02-12')
    // bỏ thêm 10–12 nhưng đó chỉ 3 ngày → vẫn chỉ ra dải 16–22
    const gaps = doKhoangVang(short, '2026-02-01', '2026-02-28', [])
    expect(gaps.map((g) => g.startISO)).toEqual(['2026-02-16'])
  })

  it('dải đang mở (chạm hôm nay, chưa có giao dịch trở lại) → không báo', () => {
    const truncated = days.filter((d) => d <= '2026-02-15')
    const gaps = doKhoangVang(truncated, '2026-02-01', '2026-02-28', [])
    expect(gaps).toEqual([])
  })

  it('dải chạm mép đầu cửa sổ → không báo (không biết nó kéo dài từ trước không)', () => {
    const tail = days.filter((d) => d >= '2026-02-05')
    // 1–4/2 trống nhưng chạm mép cửa sổ 1/2
    const gaps = doKhoangVang(tail, '2026-02-01', '2026-02-28', [])
    expect(gaps.map((g) => g.startISO)).toEqual(['2026-02-16'])
  })

  it('dải đã có hàng trips phủ (kể cả dismissed) → không báo lại', () => {
    const daDismiss = [trip({ start_on: '2026-02-16', end_on: '2026-02-22', dismissed: true })]
    expect(doKhoangVang(days, '2026-02-01', '2026-02-28', daDismiss)).toEqual([])
  })
})
```

- [ ] **Step 2: Chạy, xác nhận đỏ**

```bash
npx vitest run src/features/reports/ngayDiVang.test.ts
```

Kỳ vọng: FAIL — `Cannot find module './ngayDiVang'`.

- [ ] **Step 3: Export `monthId` từ aggregate.ts**

Dòng 159: `const monthId = ...` → `export const monthId = ...`. Chỉ thêm chữ export.

- [ ] **Step 4: Viết bản cài đặt**

Tạo `src/features/reports/ngayDiVang.ts`:

```ts
// Chuyến đi = một dải ngày TRỐNG trong sổ, không phải một khoản chi (spec 2026-09-05,
// chuyen-di). File này thuần: dò dải trống, và sinh tập ngày/tháng cần loại khỏi mốc so.
//
// Vì sao loại theo NGÀY chứ không theo THÁNG: periodCompare/forecastMonthEnd vốn làm
// việc trên mảng chi-từng-ngày ("So với trước — cùng số ngày"). Bỏ 7 ngày đi vắng, giữ
// 21 ngày thật, chính xác hơn vứt cả tháng. Riêng avg3 là số theo tháng → thangCoChuyenDi.

import { addDaysISO, monthKeyForDate } from '../../lib/dates'
import type { TripRow } from '../../types/database.types'
import { monthId } from './aggregate'

/**
 * Ngưỡng dùng ở HAI chỗ, cố ý là MỘT hằng số: (1) dải trống ≥ ngần này ngày mới đáng
 * hỏi "đi vắng?", (2) tháng có ≥ ngần này ngày đi vắng mới bị loại khỏi avg3.
 * 4 là phỏng đoán đầu tiên — trên dữ liệu thật nó cho đúng 1 câu hỏi trong 13 tháng.
 * Hỏi nhiều quá thì nâng số này, đừng thêm luật phụ.
 */
export const NGUONG_NGAY_VANG = 4

/** Tập ngày ISO nằm trong một chuyến đã xác nhận (dismissed = false), gồm cả hai đầu. */
export function ngayDiVang(trips: readonly TripRow[]): ReadonlySet<string> {
  const out = new Set<string>()
  for (const t of trips) {
    if (t.dismissed) continue
    for (let d = t.start_on; d <= t.end_on; d = addDaysISO(d, 1)) out.add(d)
  }
  return out
}

/** Bỏ các điểm rơi vào ngày đi vắng. Giữ nguyên thứ tự; tập rỗng thì trả bản sao y. */
export function boNgayDiVang<P extends { date: string }>(
  points: readonly P[],
  vang: ReadonlySet<string>,
): P[] {
  if (vang.size === 0) return [...points]
  return points.filter((p) => !vang.has(p.date))
}

/**
 * Tháng (khoá dạng `monthId` của aggregate — `${year}-${month}`, KHÔNG độn 0) có
 * ≥ NGUONG_NGAY_VANG ngày đi vắng. Tháng tài chính theo monthStartDay, không phải
 * tháng dương lịch.
 */
export function thangCoChuyenDi(
  trips: readonly TripRow[],
  monthStartDay: number,
): ReadonlySet<string> {
  const dem = new Map<string, number>()
  for (const d of ngayDiVang(trips)) {
    const k = monthId(monthKeyForDate(d, monthStartDay))
    dem.set(k, (dem.get(k) ?? 0) + 1)
  }
  const out = new Set<string>()
  for (const [k, n] of dem) if (n >= NGUONG_NGAY_VANG) out.add(k)
  return out
}

export interface KhoangVang {
  startISO: string
  endISO: string
  soNgay: number
}

/**
 * Dò các dải ≥ NGUONG_NGAY_VANG ngày liên tiếp không có giao dịch nào, trong
 * [windowStartISO, todayISO] (đều gồm).
 *
 * Ba luật im lặng, mỗi luật chặn một kiểu báo sai:
 * - Dải chạm `todayISO` → im: "chưa ghi kịp" khác "đã đi vắng"; phải có giao dịch
 *   TRỞ LẠI sau dải thì dải mới đóng.
 * - Dải chạm `windowStartISO` → im: không biết nó kéo dài từ trước cửa sổ không,
 *   báo là báo một dải cụt đầu.
 * - Dải giao với BẤT KỲ hàng trips nào (kể cả dismissed) → im: đã hỏi rồi.
 */
export function doKhoangVang(
  txDatesISO: readonly string[],
  windowStartISO: string,
  todayISO: string,
  trips: readonly TripRow[],
): KhoangVang[] {
  const coGiaoDich = new Set(txDatesISO)
  const daXet = (a: string, b: string) =>
    trips.some((t) => a <= t.end_on && b >= t.start_on)

  const out: KhoangVang[] = []
  let runStart: string | null = null
  for (let d = windowStartISO; d <= todayISO; d = addDaysISO(d, 1)) {
    if (!coGiaoDich.has(d)) {
      runStart ??= d
      continue
    }
    // ngày CÓ giao dịch → dải (nếu đang mở) vừa ĐÓNG tại d-1
    if (runStart !== null) {
      const runEnd = addDaysISO(d, -1)
      const soNgay = daysCount(runStart, runEnd)
      if (soNgay >= NGUONG_NGAY_VANG && runStart !== windowStartISO && !daXet(runStart, runEnd)) {
        out.push({ startISO: runStart, endISO: runEnd, soNgay })
      }
      runStart = null
    }
  }
  // dải còn mở khi chạm hôm nay → cố ý bỏ, xem chú thích trên đầu hàm
  return out
}

/** Số ngày của [a, b] gồm cả hai đầu. */
function daysCount(a: string, b: string): number {
  let n = 0
  for (let d = a; d <= b; d = addDaysISO(d, 1)) n++
  return n
}
```

Nếu `lib/dates.ts` đã có hàm đếm ngày kiểu `daysBetween(a, b)` (Task viết mục 1 từng thấy nó
được import ở `aggregate.ts`) thì dùng `daysBetween(a, b) + 1` thay cho `daysCount` tự chế —
mở `lib/dates.ts` xem chữ ký rồi quyết, đừng giữ hai hàm cùng nghĩa.

- [ ] **Step 5: Chạy, xác nhận xanh**

```bash
npx vitest run src/features/reports/ngayDiVang.test.ts
```

Kỳ vọng: PASS, 12 phép thử. Rồi `npx vitest run src/features/reports` để chắc `export monthId`
không đụng gì.

- [ ] **Step 6: Commit**

```bash
git add src/features/reports/ngayDiVang.ts src/features/reports/ngayDiVang.test.ts src/features/reports/aggregate.ts
git commit -m "feat(chuyen-di): toan thuan do dai ngay trong va tap loai tru"
```

---

### Task 3: Loại ngày đi vắng khỏi mốc so — avg3, Δ, dự báo

**Files:**
- Modify: `src/features/reports/aggregate.ts` (`categoryComparison` ~dòng 358,
  `monthExpenseCompare` ~dòng 594)
- Modify: `src/features/reports/monthPace.tsx` (hai chỗ gọi `forecastMonthEnd` ~dòng 184, 194)
- Modify: `src/features/reports/MonthView.tsx:203` và `:225` (truyền tham số mới)
- Modify: `src/features/bulletin/BulletinPage.tsx:138` (truyền tham số mới)
- Test: `src/features/reports/aggregate.test.ts` (thêm cases)
- Test: `tests/chuyenDiPhamVi.test.ts` (chốt canh nguồn, tạo mới)

**Interfaces:**
- Consumes: `ngayDiVang`, `thangCoChuyenDi`, `boNgayDiVang` (Task 2); `useTrips` (Task 1).
- Produces: `categoryComparison(..., thangVang?: ReadonlySet<string>)`,
  `monthExpenseCompare(..., vang?: ReadonlySet<string>)` — tham số CUỐI, mặc định tập rỗng.

- [ ] **Step 1: Viết phép thử thất bại cho aggregate**

Thêm vào `src/features/reports/aggregate.test.ts` (dùng lại helper `tx`, `RATES`, `currencyOf`
sẵn có trong file — đọc đầu file lấy đúng tên):

```ts
describe('loại ngày đi vắng khỏi mốc so (chuyến đi)', () => {
  it('categoryComparison: tháng trước có chuyến đi → deltaPct null, avg3 bỏ tháng đó', () => {
    const key: MonthKey = { year: 2026, month: 3 }
    const txs = [
      tx({ type: 'expense', amount: 900, category_id: 'food', occurred_on: '2026-03-05' }),
      tx({ type: 'expense', amount: 100, category_id: 'food', occurred_on: '2026-02-05' }), // tháng chuyến đi
      tx({ type: 'expense', amount: 500, category_id: 'food', occurred_on: '2026-01-05' }),
      tx({ type: 'expense', amount: 700, category_id: 'food', occurred_on: '2025-12-05' }),
    ]
    const thangVang = new Set(['2026-2'])
    const r = categoryComparison(txs, key, 1, currencyOf, 'JPY', RATES, null, NO_TRANSFER_CATEGORIES, thangVang)
    const food = r.rows.find((x) => x.categoryId === 'food')!
    // avg3 chỉ còn (500 + 700) / 2 = 600, KHÔNG phải (100+500+700)/3
    expect(food.avg3).toBe(600)
    // tháng liền trước là tháng chuyến đi → không so được
    expect(food.deltaPct).toBeNull()
    expect(food.isNew).toBe(false)
  })

  it('categoryComparison: không truyền thangVang → y hệt hành vi cũ', () => {
    const key: MonthKey = { year: 2026, month: 3 }
    const txs = [
      tx({ type: 'expense', amount: 900, category_id: 'food', occurred_on: '2026-03-05' }),
      tx({ type: 'expense', amount: 100, category_id: 'food', occurred_on: '2026-02-05' }),
    ]
    const r = categoryComparison(txs, key, 1, currencyOf, 'JPY', RATES)
    const food = r.rows.find((x) => x.categoryId === 'food')!
    expect(food.deltaPct).toBe(800)
  })

  it('monthExpenseCompare: ngày đi vắng bị bỏ khỏi CẢ HAI vế', () => {
    const key: MonthKey = { year: 2026, month: 3 }
    // Tháng 2 (kỳ trước): chi 100/ngày ở 1–15/2, đi vắng 16–22/2 (0), rồi 100/ngày 23–28.
    // Tháng 3 (kỳ xem, đã QUA): chi 100/ngày cả tháng.
    const txs: TransactionRow[] = []
    for (let d = 1; d <= 28; d++) {
      const iso = `2026-02-${String(d).padStart(2, '0')}`
      if (iso >= '2026-02-16' && iso <= '2026-02-22') continue
      txs.push(tx({ type: 'expense', amount: 100, category_id: 'food', occurred_on: iso }))
    }
    for (let d = 1; d <= 31; d++) {
      txs.push(tx({ type: 'expense', amount: 100, category_id: 'food', occurred_on: `2026-03-${String(d).padStart(2, '0')}` }))
    }
    const vang = new Set<string>()
    for (let d = 16; d <= 22; d++) vang.add(`2026-02-${d}`)

    const khongLoai = monthExpenseCompare(txs, key, 1, '2026-04-15', currencyOf, 'JPY', RATES)!
    const coLoai = monthExpenseCompare(txs, key, 1, '2026-04-15', currencyOf, 'JPY', RATES, NO_TRANSFER_CATEGORIES, vang)!
    // Không loại: kỳ trước trọn tháng = 2.100 (21 ngày × 100). Có loại: vẫn 2.100 nhưng
    // trên 21 NGÀY THẬT — nên "cùng số ngày" thôi bị 7 ngày số 0 kéo lệch.
    // Đừng khẳng định giá trị nội bộ của PeriodCompare mà mình chưa đọc — khẳng định
    // điều chắc chắn: hai kết quả PHẢI khác nhau, và bản có loại không nhỏ hơn.
    expect(coLoai).not.toEqual(khongLoai)
  })
})
```

**Chú ý khi viết test này:** mở `PeriodCompare` trong `periodCompare.ts` xem field thật
(`spent`, `priorSameDays`, `priorFull`…) rồi thay khẳng định cuối bằng khẳng định CỤ THỂ trên
field kỳ-trước (ví dụ `coLoai.priorSameDays` phải LỚN HƠN `khongLoai.priorSameDays` vì 7 ngày
0 đã bị bỏ). Phép thử "not.toEqual" ở trên chỉ là sàn tối thiểu — nâng nó lên khi đã đọc kiểu.

- [ ] **Step 2: Chạy, xác nhận đỏ**

```bash
npx vitest run src/features/reports/aggregate.test.ts
```

Kỳ vọng: FAIL — tham số thứ 9 chưa tồn tại (TS2554) hoặc kết quả chưa loại.

- [ ] **Step 3: Sửa `categoryComparison`**

Thêm tham số cuối:

```ts
  transferIds: TransferIds = NO_TRANSFER_CATEGORIES,
  /** Khoá tháng (monthId) có chuyến đi — bỏ khỏi avg3 và Δ. Rỗng = hành vi cũ. */
  thangVang: ReadonlySet<string> = KHONG_THANG_VANG,
```

với `const KHONG_THANG_VANG: ReadonlySet<string> = new Set()` ở đầu file, cạnh
`NO_TRANSFER_CATEGORIES` (đọc xem hằng đó khai thế nào, khai giống hệt).

Trong vòng tính rows, thay khối avg3/deltaPct/isNew:

```ts
    const thisMonth = inner.get(m0) ?? 0
    const prevMonth = inner.get(m1) ?? 0
    // Tháng có chuyến đi không được làm mốc: bỏ khỏi CẢ tử lẫn mẫu của avg3. Mẫu rỗng
    // (cả 3 tháng đều có chuyến) → avg3 = 0, cùng nghĩa với danh mục chưa có lịch sử.
    const mocSo = [m1, m2, m3].filter((m) => !thangVang.has(m))
    const avg3 =
      mocSo.length === 0
        ? 0
        : Math.round(mocSo.reduce((s, m) => s + (inner.get(m) ?? 0), 0) / mocSo.length)
    if (thisMonth === 0 && prevMonth === 0 && avg3 === 0) continue
    // Tháng liền trước là tháng chuyến đi → không có mốc để so: Δ null (bảng in "—"),
    // và cũng KHÔNG được nói "mới" — danh mục không mới, chỉ là mốc so bị thiếu.
    const prevLaMoc = !thangVang.has(m1)
    const deltaPct =
      prevLaMoc && prevMonth > 0 ? Math.round(((thisMonth - prevMonth) / prevMonth) * 100) : null
    const isNew = prevLaMoc && prevMonth === 0 && thisMonth > 0
```

- [ ] **Step 4: Sửa `monthExpenseCompare`**

Thêm tham số cuối `vang: ReadonlySet<string> = KHONG_NGAY_VANG` (hằng riêng, cùng khuôn).
Đổi `dailyOf` để trả **points** (giữ `date`), rồi lọc:

```ts
  const dailyOf = (r: { start: string; end: string }) => {
    const lastISO = addDaysISO(r.end, -1)
    return dailyExpenseTotals(
      txs.filter((t) => t.occurred_on >= r.start && t.occurred_on < r.end),
      r.start,
      lastISO,
      currencyOf,
      base,
      rates,
      transferIds,
    ).points
  }

  const curPts = boNgayDiVang(dailyOf(cur), vang)
  const prevPts = boNgayDiVang(dailyOf(prev), vang)
  // Ngày đã trôi cuối cùng của kỳ xem — để đếm lại daysElapsed SAU khi loại.
  const lastElapsedISO = daysElapsed === 0 ? null : addDaysISO(cur.start, daysElapsed - 1)
  const daysElapsedKeep =
    lastElapsedISO === null ? 0 : curPts.filter((p) => p.date <= lastElapsedISO).length

  return periodCompare({
    current: curPts.map((p) => p.expense),
    prior: prevPts.map((p) => p.expense),
    daysElapsed: daysElapsedKeep,
    daysInPeriod: curPts.length,
  })
```

Import `boNgayDiVang` từ `./ngayDiVang`. (Không có vòng import: ngayDiVang chỉ import
`monthId` — một hằng — từ aggregate; nếu tsc than vòng, chuyển `monthId` ra `lib/dates.ts`
và sửa cả hai chỗ import.)

- [ ] **Step 5: Chạy, xác nhận xanh — CẢ file, không chỉ test mới**

```bash
npx vitest run src/features/reports/aggregate.test.ts
```

Mọi phép thử cũ phải xanh nguyên. Đỏ một cái cũ = mặc-định-rỗng chưa thật sự là hành vi cũ —
sửa code, không sửa test cũ.

- [ ] **Step 6: Dự báo — loại ngày đi vắng trong `useMonthPace`**

Trong `monthPace.tsx`, hook `useMonthPace`: thêm

```ts
  const { data: trips = [] } = useTrips()
  const vang = useMemo(() => ngayDiVang(trips), [trips])
  // Số ngày THƯỜNG (không đi vắng) của trọn tháng — mẫu số mới của dự báo.
  const nKeepMonth = useMemo(
    () => monthDaily.points.filter((p) => !vang.has(p.date)).length,
    [monthDaily, vang],
  )
```

và đổi HAI chỗ gọi `forecastMonthEnd` (dòng ~184 và ~194) theo cùng một khuôn:

```ts
  const vpKeep = variableDaily.points.slice(0, daysElapsed).filter((p) => !vang.has(p.date))
  const forecast = isCurrentMonth
    ? forecastMonthEnd(spentSoFar, vpKeep.length, nKeepMonth, vpKeep.map((p) => p.expense), fixedSoFar)
    : null
```

(chỗ thứ hai thay `variableDaily` → `budgetVariableDaily`, `spentSoFar` → `budgetSpentSoFar`,
`fixedSoFar` → `budgetFixedSoFar`, giữ nguyên điều kiện `budgetVariableDaily !== null`).

`monthDaily`/`budgetDaily` cho BIỂU ĐỒ giữ nguyên không lọc — chỉ dự báo đổi đầu vào.

- [ ] **Step 7: Nối ba call site**

`MonthView.tsx`: thêm `const { data: trips = [] } = useTrips()` +
`const vang = useMemo(() => ngayDiVang(trips), [trips])` +
`const thangVang = useMemo(() => thangCoChuyenDi(trips, monthStartDay), [trips, monthStartDay])`,
rồi:

- dòng ~203: `monthExpenseCompare(rangeTxs, monthKey, monthStartDay, todayISO, currencyOf, base, r, transferIds, vang)` — thêm `vang` (và `trips` vào deps của useMemo)
- dòng ~225: `categoryComparison(rangeTxs, monthKey, monthStartDay, currencyOf, base, r, cutoffDay, transferIds, thangVang)` — thêm `thangVang` (+ deps)

`BulletinPage.tsx` dòng ~138: cùng khuôn — mở chỗ gọi xem nó đang truyền `transferIds` chưa
(bắt buộc phải rồi, vì đó là tham số trước); thêm `vang` sau nó, và thêm `useTrips` +
`ngayDiVang` vào phần data của trang.

- [ ] **Step 8: Chốt canh nguồn**

Tạo `tests/chuyenDiPhamVi.test.ts` — cùng khuôn `tests/chiChuaGhiPhamVi.test.ts` (đọc file đó,
chép cách khai ROOT):

```ts
// Canh tham số loại-trừ của chuyến đi (spec 2026-09-05-chuyen-di §8).
//
// `vang`/`thangVang` có MẶC ĐỊNH RỖNG để test cũ khỏi sửa — nghĩa là QUÊN TRUYỀN không
// gây lỗi biên dịch, chỉ gây hai màn nói hai con số. Đúng cái bẫy categoryKind.test.ts
// chặn cho transferIds; đây là bản cho chuyến đi.
import { readFileSync } from 'node:fs'
import { join } from 'node:path'
import { fileURLToPath } from 'node:url'
import { describe, expect, it } from 'vitest'

const ROOT = fileURLToPath(new URL('..', import.meta.url))

const PHAI_TRUYEN: [file: string, hàm: string, thamSố: string][] = [
  [join('src', 'features', 'reports', 'MonthView.tsx'), 'monthExpenseCompare(', 'vang'],
  [join('src', 'features', 'reports', 'MonthView.tsx'), 'categoryComparison(', 'thangVang'],
  [join('src', 'features', 'bulletin', 'BulletinPage.tsx'), 'monthExpenseCompare(', 'vang'],
]

describe('phạm vi chuyến đi', () => {
  for (const [rel, ham, thamSo] of PHAI_TRUYEN) {
    it(`${rel} gọi ${ham}…) có truyền ${thamSo}`, () => {
      const src = readFileSync(join(ROOT, rel), 'utf8')
      const idx = src.indexOf(ham)
      expect(idx).toBeGreaterThan(-1)
      // đối số phải xuất hiện trong vòng 400 ký tự sau chỗ gọi — đủ phủ một lời gọi
      // nhiều dòng, đủ hẹp để không ăn nhầm lời gọi khác
      expect(src.slice(idx, idx + 400)).toContain(thamSo)
    })
  }

  it('demoRepo seed KHÔNG có sẵn chuyến nào — demo mở ra phải y hệt hôm nay', () => {
    const src = readFileSync(join(ROOT, join('src', 'data', 'demoRepo.ts')), 'utf8')
    const seedTrips = src.match(/const trips: TripRow\[\] = \[[^\]]+\]/)
    expect(seedTrips).toBeNull()
  })
})
```

- [ ] **Step 9: Kiểm toàn bộ và commit**

```bash
npx tsc -b
npx vitest run
npm run lint
git add src/features/reports/aggregate.ts src/features/reports/aggregate.test.ts src/features/reports/monthPace.tsx src/features/reports/MonthView.tsx src/features/bulletin/BulletinPage.tsx tests/chuyenDiPhamVi.test.ts
git commit -m "feat(chuyen-di): loai ngay di vang khoi avg3, delta va du bao"
```

---

### Task 4: Dò và hỏi — luật thông báo + thẻ xác nhận ở tab Dài hạn

**Files:**
- Create: `src/features/notifications/rules/tripRules.ts`
- Create: `src/features/notifications/rules/tripRules.test.ts`
- Create: `src/features/reports/TripGapCard.tsx`
- Modify: `src/features/notifications/types.ts` (union `NotificationType` + một mục
  `NOTIFICATION_META` + field `trips?: TripRow[]` trong `NotificationInput`)
- Modify: `src/features/notifications/rules.ts` (~dòng 86, thêm `...tripRules(input)`)
- Modify: `src/features/notifications/useNotifications.ts` (thêm `useTrips`, đưa vào input +
  `inputsReady`)
- Modify: `src/features/reports/LongView.tsx` (render `<TripGapCard>` trên đầu, sau header)

**Interfaces:**
- Consumes: `doKhoangVang`, `NGUONG_NGAY_VANG`, `KhoangVang` (Task 2); `useTrips`,
  `useCreateTrip` (Task 1); `RECENT_TXS_DAYS` từ `notifications/types`.
- Produces: type `'trip-gap'` trong `NotificationType`; component
  `TripGapCard({ txs, windowStartISO, todayISO })`.

- [ ] **Step 1: Viết phép thử thất bại cho luật**

Tạo `src/features/notifications/rules/tripRules.test.ts`. **Mở `dataRules.test.ts` trước** và
chép cách nó dựng `NotificationInput` mẫu (thường có helper `input()` hoặc hằng BASE) — dưới
đây chỉ viết phần khác biệt:

```ts
describe('tripGapRule', () => {
  // dựng recentTxs có giao dịch mỗi ngày trong 30 ngày gần nhất TRỪ dải 10–16 ngày trước
  it('dải 7 ngày trống đã đóng → đúng 1 thông báo action', () => {
    const n = tripRules(inputVoiDaiTrong())
    expect(n).toHaveLength(1)
    expect(n[0].type).toBe('trip-gap')
    expect(n[0].kind).toBe('action')
    expect(n[0].to).toBe('/reports?view=long')
  })

  it('trips chưa tải (undefined) → im lặng, không đoán', () => {
    expect(tripRules({ ...inputVoiDaiTrong(), trips: undefined })).toEqual([])
  })

  it('dải đã có hàng trips phủ → im', () => {
    const daXet = [tripMau({ dismissed: true })] // phủ đúng dải trống
    expect(tripRules({ ...inputVoiDaiTrong(), trips: daXet })).toEqual([])
  })
})
```

- [ ] **Step 2: Chạy, xác nhận đỏ**

```bash
npx vitest run src/features/notifications/rules/tripRules.test.ts
```

- [ ] **Step 3: types.ts — ba chỗ**

1. Union: thêm `| 'trip-gap'` vào `NotificationType`.
2. `NOTIFICATION_META['trip-gap']` — chép đúng cấu trúc mục `'recurring-suggestion'` (một
   action dạng gợi ý, không phải cảnh báo). Nội dung:

```ts
  'trip-gap': {
    cta: 'Xem lại',
    badge: 'ĐI VẮNG?',
    source: 'Sổ · dải ngày trống',
    kind: 'action',
    label: 'Dải ngày không có giao dịch nào',
    hint: 'Đánh dấu là chuyến đi thì các phép so sánh sẽ bỏ những ngày này ra, tháng đó thôi trông rẻ giả.',
  },
```

3. `NotificationInput`: thêm

```ts
  /** Chuyến đi đã lưu (kể cả dismissed). undefined = chưa tải xong; luật chuyến đi im. */
  trips?: TripRow[]
```

- [ ] **Step 4: tripRules.ts**

```ts
// Dải ngày trống dài → hỏi "đi vắng?". Vì sao dò thay vì bắt người dùng nhớ ngày:
// chính người dùng nhớ chuyến Tết 2026 là "tháng 12 hoặc 1" trong khi dữ liệu chỉ ra
// 16–22/2 — lệch ba tháng. Khoảng trống thì máy đọc được, trí nhớ thì không.
//
// Luật này chỉ nhìn recentTxs (RECENT_TXS_DAYS ngày) — dải cũ hơn do TripGapCard ở tab
// Dài hạn lo, nơi cả năm giao dịch đã nằm sẵn trong tay, không tốn truy vấn mới nào.
import { addDaysISO } from '../../../lib/dates'
import { doKhoangVang } from '../../reports/ngayDiVang'
import { RECENT_TXS_DAYS, type AppNotification, type NotificationInput } from '../types'

export function tripRules(input: NotificationInput): AppNotification[] {
  const { trips, recentTxs, todayISO } = input
  if (!trips) return []
  const windowStart = addDaysISO(todayISO, -RECENT_TXS_DAYS)
  const gaps = doKhoangVang(
    recentTxs.map((t) => t.occurred_on),
    windowStart,
    todayISO,
    trips,
  )
  return gaps.map((g) => ({
    key: `trip-gap:${g.startISO}`,
    kind: 'action' as const,
    type: 'trip-gap' as const,
    severity: 'info' as const,
    title: `${g.soNgay} ngày không có giao dịch nào (${g.startISO} → ${g.endISO}) — đi vắng?`,
    detail: 'Đánh dấu là chuyến đi thì các phép so sánh bỏ những ngày này ra.',
    onISO: g.startISO,
    to: '/reports?view=long',
  }))
}
```

Hai chỗ PHẢI đối chiếu với code thật trước khi giữ nguyên: (1) `severity: 'info'` — mở
`NotificationSeverity` trong types.ts, nếu không có `'info'` thì dùng đúng mức mà
`'recurring-suggestion'` đang dùng; (2) định dạng ngày trong `title` — nếu `lib/dates` có
`dayMonthLabel` thì dùng nó thay ISO thô (`16/2 → 22/2` đọc được hơn `2026-02-16`).

- [ ] **Step 5: Đăng ký luật + nối input**

`rules.ts` dòng ~86: thêm `...tripRules(input),` cạnh `...dataRules(input),` (nhớ import).
`useNotifications.ts`: thêm `const tripsQ = useTrips()` cạnh các query khác, đưa
`trips: tripsQ.data` vào object input, và đưa `tripsQ` vào cùng cơ chế `inputsReady` mà các
query khác đang dùng (đọc cách `goalsQ` được xử, làm y vậy).

- [ ] **Step 6: Chạy test luật — xanh. Rồi TripGapCard**

Tạo `src/features/reports/TripGapCard.tsx`:

```tsx
// Thẻ xác nhận chuyến đi ở tab Dài hạn — nơi duy nhất có sẵn CẢ NĂM giao dịch trong
// tay, nên dò được cả chuyến cũ (chuyến Tết 2/2026 nằm ngoài cửa sổ 90 ngày của luật
// thông báo). Không tốn truy vấn mới: nhận txs từ chính LongView.
//
// Mỗi lúc một câu hỏi (gaps[0]): trả lời xong dải này, invalidate trips → dải sau tự
// hiện. Hai dải cùng lúc là bắt người dùng đọc một danh sách thay vì trả lời một câu.
import { Card } from '../../components/ui'
import { useCreateTrip, useTrips } from '../../hooks/queries'
import type { TransactionRow } from '../../types/database.types'
import { doKhoangVang } from './ngayDiVang'

export function TripGapCard({
  txs,
  windowStartISO,
  todayISO,
}: {
  txs: readonly TransactionRow[]
  windowStartISO: string
  todayISO: string
}) {
  const { data: trips } = useTrips()
  const createTrip = useCreateTrip()
  if (!trips) return null
  const gaps = doKhoangVang(txs.map((t) => t.occurred_on), windowStartISO, todayISO, trips)
  if (gaps.length === 0) return null
  const g = gaps[0]

  const traLoi = (dismissed: boolean) =>
    createTrip.mutate({ start_on: g.startISO, end_on: g.endISO, dismissed })

  return (
    <Card as="section" elevation="panel" padding="panel">
      <p className="text-sm text-fg-primary">
        {/* nội dung + 2 nút — xem Step 7 */}
      </p>
    </Card>
  )
}
```

- [ ] **Step 7: Ruột thẻ — chép khuôn nút từ ReconcileSheet**

Mở `ReconcileSheet.tsx` phần cuối (cặp nút "Hủy" / "Đã đối chiếu") và chép NGUYÊN class của
hai nút đó (một nút thường, một nút chính) — đừng tự chế nút mới, `tests/designSystem.test.ts`
ban nút nền xanh tự viết. Nội dung thẻ:

- Câu hỏi: `{g.soNgay} ngày không có giao dịch nào ({nhãn ngày}) — anh đi vắng?` — số ngày qua
  `<Num>`.
- Nút chính: **Đánh dấu là chuyến đi** → `traLoi(false)`
- Nút thường: **Không phải, đừng hỏi lại** → `traLoi(true)`
- Disable cả hai khi `createTrip.isPending`.

Trong `LongView.tsx`: render `<TripGapCard txs={txs} windowStartISO={range.start}
todayISO={todayISO} />` ngay trên khối đầu tiên của tab (sau header, trước card đầu).
`todayISO` — xem biến sẵn có của LongView (nó đã có `todayISO` cho `anchor`).

- [ ] **Step 8: Kiểm và commit**

```bash
npx tsc -b
npx vitest run
npm run lint
git add src/features/notifications src/features/reports/TripGapCard.tsx src/features/reports/LongView.tsx
git commit -m "feat(chuyen-di): tu do dai ngay trong roi hoi — luat thong bao + the o tab Dai han"
```

---

### Task 5: Chú thích tháng + dấu trên biểu đồ + đường bỏ đánh dấu

**Files:**
- Create: `src/features/reports/GhiChuChuyenDi.tsx`
- Modify: `src/features/reports/MonthView.tsx` (render trong ReportBlock `m-danh-muc`, trước
  `<MonthCategoryTable>` ~dòng 618)
- Modify: `src/features/reports/MonthlyBarsCard.tsx` (prop `markedKeys`)
- Modify: `src/features/reports/LongView.tsx` (truyền `markedKeys` nếu nó dùng MonthlyBarsCard
  — grep `<MonthlyBarsCard` để tìm mọi chỗ gọi)

**Interfaces:**
- Consumes: `ngayDiVang`, `monthId` (Task 2); `useTrips`, `useDeleteTrip` (Task 1); biến
  `range`, `trips`, `vang` đã có trong MonthView từ Task 3.
- Produces: `<GhiChuChuyenDi trips={...} range={...} />`;
  `MonthlyBarsCard({ ..., markedKeys?: ReadonlySet<string> })` với khoá là chuỗi `monthId`.

- [ ] **Step 1: GhiChuChuyenDi**

```tsx
// Chú thích tháng có chuyến đi + đường lùi duy nhất (bỏ đánh dấu). KHÔNG bọc <Guide>:
// Guide biến mất ở chế độ Gọn (mặc định), mà câu này là cảnh báo dữ-liệu-không-so-được —
// đúng nhóm "ĐỪNG bọc" trong chú thích đầu Guide.tsx. Bài học từ ChiChuaGhiLine (mục 1).
import { Num } from '../../components/ui'
import { useDeleteTrip } from '../../hooks/queries'
import type { TripRow } from '../../types/database.types'

export function GhiChuChuyenDi({
  trips,
  range,
}: {
  trips: readonly TripRow[]
  range: { start: string; end: string }
}) {
  const deleteTrip = useDeleteTrip()
  // Chuyến giao với kỳ đang xem (end loại trừ nên so với end − 1 qua chuỗi ISO)
  const trongKy = trips.filter(
    (t) => !t.dismissed && t.start_on < range.end && t.end_on >= range.start,
  )
  if (trongKy.length === 0) return null
  const soNgay = trongKy.reduce((s, t) => {
    // đếm phần nằm TRONG kỳ, chuyến vắt tháng chỉ tính phần của tháng này
    let n = 0
    for (let d = t.start_on; d <= t.end_on; d = addDaysISO(d, 1))
      if (d >= range.start && d < range.end) n++
    return s + n
  }, 0)

  return (
    <p className="mb-2 text-sm text-fg-warn">
      <Num tone="neutral" className="text-inherit">{soNgay}</Num> ngày đi vắng trong kỳ —
      tổng Chi không so được với tháng thường.{' '}
      <button
        type="button"
        className="underline underline-offset-2"
        onClick={() => trongKy.forEach((t) => deleteTrip.mutate(t.id))}
        disabled={deleteTrip.isPending}
      >
        Bỏ đánh dấu
      </button>
    </p>
  )
}
```

(nhớ import `addDaysISO`; nếu `<Num>` không nhận `className` thì bỏ wrapper, xem chữ ký thật.)

Render trong MonthView, dòng đầu của ReportBlock `m-danh-muc`:

```tsx
          <ReportBlock id="m-danh-muc" no="02" title="Chi tiêu đi vào đâu">
            <GhiChuChuyenDi trips={trips} range={range} />
            <MonthCategoryTable
```

- [ ] **Step 2: Dấu trên biểu đồ 12 tháng**

`MonthlyBarsCard.tsx`: thêm prop `markedKeys?: ReadonlySet<string>` (khoá = `monthId(p.key)`).
Trong `barData`, thêm `marked: markedKeys?.has(monthId(p.key)) ?? false`. Rồi:

1. **Mở file, đếm số `<Bar>`** trong JSX (thu và chi). Với MỖI `<Bar>`, thêm map `<Cell>`:

```tsx
   {barData.map((d, i) => (
     <Cell key={i} fillOpacity={d.marked ? 0.45 : 1} />
   ))}
```

   `Cell` import từ `'recharts'`. Cột tháng chuyến đi MỜ ĐI chứ không biến mất — spec §2
   quyết định 4 cấm ẩn cột.

2. Dưới biểu đồ, khi có ít nhất một tháng marked, một dòng chú giải (KHÔNG bọc Guide):

```tsx
   {barData.some((d) => d.marked) && (
     <p className="mt-1 text-2xs text-fg-muted">Cột mờ = tháng có chuyến đi, không so được với tháng thường.</p>
   )}
```

Chỗ gọi (grep `<MonthlyBarsCard` — MonthView ~dòng 630 và có thể LongView): truyền
`markedKeys={thangVang}`... **không đúng** — `thangVang` là tháng ĐẠT NGƯỠNG 4 ngày, còn dấu
trên biểu đồ nên hiện cho MỌI tháng có ngày đi vắng? Không: dùng ĐÚNG `thangVang` — cùng một
định nghĩa "tháng có chuyến đi" cho cả loại-avg3 lẫn dấu, hai định nghĩa là hai nguồn cãi nhau.
Truyền `markedKeys={thangVang}` ở mọi chỗ gọi. LongView chưa có `thangVang` thì dựng bằng
`thangCoChuyenDi(trips, monthStartDay)` với `useTrips` — LongView đã có `monthStartDay`.

- [ ] **Step 3: Kiểm và commit**

```bash
npx tsc -b
npx vitest run
npm run lint
git add src/features/reports/GhiChuChuyenDi.tsx src/features/reports/MonthView.tsx src/features/reports/MonthlyBarsCard.tsx src/features/reports/LongView.tsx
git commit -m "feat(chuyen-di): chu thich thang, dau tren bieu do, nut bo danh dau"
```

---

### Task 6: Xem bằng mắt, detect_changes, chốt

**Files:** không sửa gì trừ khi phát hiện lỗi.

- [ ] **Step 1: Mở app demo** (`preview_start` với `so-chi-tieu-demo` — KHÔNG dùng Bash).

- [ ] **Step 2: Dựng kịch bản thật bằng chính tính năng của app**

Demo seed không có dải trống. Tạo một dải bằng đường người dùng thật: vào **Sổ** (`/so`),
lọc một khoảng 5–6 ngày liên tiếp cách đây ~2 tuần, **xóa hàng loạt** giao dịch trong khoảng
đó (tính năng bulk delete có sẵn). Reload.

- [ ] **Step 3: Kiểm tám điểm**

1. **Bản tin** hiện việc "… ngày không có giao dịch nào — đi vắng?" (dải nằm trong 90 ngày).
2. Bấm vào → sang tab **Dài hạn**, thấy `TripGapCard` hỏi đúng dải.
3. Bấm **Đánh dấu là chuyến đi** → thẻ biến mất; tab **Tháng này** của tháng chứa dải hiện
   chú thích "N ngày đi vắng…"; cột Δ/TB 3 th của tháng SAU nó đổi (mốc so đã loại).
4. Biểu đồ 12 tháng: cột tháng đó **mờ**, có dòng chú giải, KHÔNG biến mất.
5. **Bỏ đánh dấu** ở chú thích → mọi thứ trở về như trước khi đánh dấu.
6. Đánh dấu lại, rồi thử nút **Không phải, đừng hỏi lại** trên một dải khác (tạo thêm một dải
   bằng bulk delete) → thẻ biến mất và KHÔNG quay lại sau reload.
7. Chế độ **Sáng** và **375px cỡ chữ 1,25×**: chú thích + thẻ hỏi + chú giải biểu đồ đọc
   được, không tràn. (Chú ý: cả ba đều cấm bọc `Guide` — nhìn thấy ở chế độ Gọn mặc định.)
8. Console không có lỗi (`read_console_messages`).

- [ ] **Step 4: Index lại và soát phạm vi**

```bash
node .gitnexus/run.cjs analyze
```

Rồi `detect_changes({scope: "compare", base_ref: "master"})` — phạm vi phải nằm trong:
migration + `database.types.ts` + `repo.ts` + `supabaseRepo.ts` + `demoRepo.ts` + `queries.ts`
+ `features/reports/*` + `features/notifications/*` + `features/bulletin/BulletinPage.tsx`.
**Không** được thấy `src/mcp/`. (`analyze` sẽ tự sửa số symbol trong CLAUDE.md/AGENTS.md —
commit riêng `chore(gitnexus)` như lệ.)

- [ ] **Step 5: Migration production**

Bảng `trips` phải tồn tại trên Supabase thật trước khi merge được deploy. Hỏi user đường áp
migration họ vẫn dùng (ghi nhớ deploy nằm ngoài repo) — KHÔNG tự áp vào production.

- [ ] **Step 6: Commit sửa chữa nếu có, rồi bàn giao theo finishing-a-development-branch.**

---

## Tự soát kế hoạch

**Phủ spec:** §4 bảng trips → Task 1 · §5.1 dò + hỏi + nhớ lần bỏ qua → Task 2 (detector) +
Task 4 (luật + thẻ) · §5.2 loại mốc so → Task 3 · §5.3 chú thích + dấu biểu đồ → Task 5 ·
§6 ca biên → phép thử Task 2/3 · §8 chốt canh → Task 3 Step 8. Spec §5.1 "dải đang mở không
hỏi" và "dải chạm mép cửa sổ không hỏi" → phép thử doKhoangVang.

**Điểm cố ý để mở (phải tra tại chỗ, không đoán):** tên hàm ghi store của demoRepo (Step 5
Task 1) · cấu trúc `InsertOf` (Step 2 Task 1) · field thật của `PeriodCompare` để nâng phép
thử `not.toEqual` (Task 3 Step 1) · `NotificationSeverity` có `'info'` không (Task 4) · số
`<Bar>` trong MonthlyBarsCard (Task 5) · cách LongView đặt `todayISO`. Mỗi điểm đều ghi rõ
mở file nào.

**Nhất quán tên:** `ngayDiVang` / `boNgayDiVang` / `thangCoChuyenDi` / `doKhoangVang` /
`NGUONG_NGAY_VANG` / `KhoangVang` / `TripRow` / `NewTrip` / `useTrips` / `useCreateTrip` /
`useDeleteTrip` / `TripGapCard` / `GhiChuChuyenDi` / `'trip-gap'` — dùng đúng bộ này ở cả 6
task.
